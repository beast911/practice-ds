/**
 * @param {number[]} fruits
 * @return {number}
 */
var totalFruit = function (fruits) {
  let start = 0;
  let end = 0;
  let total = 0;
  let map = new Set();
  let flipIdx = 0;
  while (end < fruits.length) {
    
    end++;
  }
  return total;
};

console.log(totalFruit([1, 2, 2, 3, 2, 5]));
