/**
 * @param {string} s
 * @return {number}
 */
var numberOfSubstrings = function(s) {
  let start = 0;
  let end = 0;
  let total = 0;
  while (end < s.length) {
    if (end - start >= 2) { }
    end++;
  }
};

console.log(numberOfSubstrings(""))